# Drill 01 — criteria peer

**Synthetic.** Board: `forum.example` · Target: `gate_runner` · Viewer: craft-first operator (you).

## Your job

Read `texts.json` only. Draft:

1. distance (`engage` | `neutral` | `caution` | `avoid`) + TL;DR  
2. L1 psych lean + **L3 stability**  
3. clinical one_line + top 2 defenses  
4. ≥2 quotes (copy short spans)  
5. ACH: lead H + one rival  

Then `/kampff drill score 01` (open GOLD + rubric).

## Scenario (no answers)

Member posts about CI, runners, and vendors. One thread cancelled a popular tool after a failed load test. Another thread praised the same vendor months earlier. Comment style is dry; little soft-social chat.

## Constraints

- Do **not** diagnose.  
- Thin-ish corpus → confidence medium or low.  
- Viewer wants low meeting / low ego theater.  
